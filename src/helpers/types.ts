export type HeroIds = {
  superman: string;
  batman: string;
  wonderwoman: string;
  flash: string;
  greenlantern: string;
  aquaman: string;
  greenarrow: string;
  cyborg: string;
};

export type HeroMenuData = {
  id: string;
  name: string;
  active: boolean;
};
